# Digital Biotope — 作業の前提

食物連鎖のシミュレータ。「どういう条件なら多様な生態系が成り立つのか」を
実験で確かめていくのが目的で、動くものを作ること自体は目的ではない。

- **全体像: [docs/OVERVIEW.md](docs/OVERVIEW.md)** — 何が分かっていて、何が穴なのか。最初に読む
- 使い方とモデル: [README.md](README.md)
- 実験の記録: [docs/reports/](docs/reports/)
- **これからの方針: [docs/DIRECTION.md](docs/DIRECTION.md)** — 何を目指していて、次に何をやるか

## このプロジェクトで何度も痛い目を見たこと

21本のレポートで、**書いた結論のうち7件が測り直しで覆っている**。
以下は全部その実例から出たもので、守らないと同じ失敗をする。

### 1. 眺めて分かることは何も無い

6構成のうち4つが最初に書いたとき壊れていた。4つとも型検査を通り、
画面上では動いていた。「雑食動物が元気に増えている」ようにしか見えない裏で
草食動物が絶滅していた。**個体数の内訳を数字で見るまで、壊れているかは分からない。**

### 2. 平均ではなく谷を見る

絶滅は平均では予測できない。**振れ幅の下端が0に当たった瞬間に決まる。**
01・04・06・07・09 が全部この形をしている。表には必ず `平均(最小-最大)` を出す。

上位捕食者ほどこれが効く。個体数の桁が小さい（谷で1〜5体）ので、
パラメータではなく偶然だけで消える。

### 3. 3シードでは偶然と本物が区別できない

04で3シードから非単調な機構を「発見」しかけ、8シードで消えた。
**既定は8シード**。5シードで `5/5` でも8シードで崩れることがある（4層がそれ）。

そして `7/8` と `8/8` の差は偶然の範囲。読み分けてはいけない。
条件をまたいで束ねる（例: 4条件で 30/32）なら意味が出る。

### 4. 動かした覚えのない軸が動いていないか疑う

**これが一番やられている。**

- 06: 探索の途中経過をそのまま結論にした。複数の軸が同時に動いていた
- 08: 死骸の還元は回復速度をいじらないので「エネルギー量は同じはず」と思った。
  実際には毎ステップ数百のエネルギーを注ぎ込んでいた

対策は**測る仕組みを先に作ること**。実際に草へ入った量（`grassAdded`）と
死骸から戻った量（`grassFromCorpses`）を分けて数える仕組みがあるおかげで、
08の交絡に気づけた。新しい軸を足すときは、**それが何を一緒に動かすかを
数える手段**も一緒に作る。

### 5. 操作は「1つだけ動かせる形」に設計する

07のパッチが有効だったのは、重みの平均をちょうど1に正規化して
**総生産量を変えずに分布だけを変える**ようにしたから。
09の撒き散らしも、総入力と流入のばらつきを動かさずに空間の集中だけを変えた。

逆に09の在庫（放出率）は、下げると変動係数も総入力も同時に動いてしまい、
何も結論できなかった。**設計の段階で「これは何を固定できるのか」を決める。**

### 6. 崩壊した試行を統計に混ぜない

09で見つけた罠。崩壊すると捕食者が消えて死骸の流入が0で固まるので、
変動係数が跳ね上がる。**「変動係数が高いから崩壊した」ではなく
「崩壊したから高い」**を測っていた。

崩壊は測定対象の挙動そのものを変える。崩壊後のデータを含む指標は、
結果が原因に見える形に化ける。

**崩壊率は走行の長さで変わる。** 14で踏んだ。1回の大きな撹乱ごとに
崩壊が起きうる操作では、確率は走行が長いほど積み上がる。
8000ステップで 8/8 だった条件が90000ステップでは 2/8 になった。
**短い走行の生存率を見て「この条件は安全」と判断しないこと。**
使う予定の長さで確かめる。

### 7. 共存の主張には既定の条件では足りない

**6000ステップ / 8シードは共存の判定には短すぎる。** 21で確定した。
07 がパッチ下で `8/8` と出した条件は、30000ステップでは `3/8` に落ちる
（絶滅は14365〜27395歩で起きていた）。少数派が10体前後で生き延びている状態は、
短い走行では共存に見える。

そして「両方生き残った」は共存の証拠にならない。**遅い排除の途中**かもしれない。
共存を主張するなら**相互侵入**を測る——片方が平衡にいるところへ相手を5体だけ入れて、
両方向で増えるか。片方向だけなら途中経過。
**どちらから始めても同じ平衡に戻る**なら、内点の安定平衡がある。

逆に、既定の条件で出した過去の共存・崩壊の判定は**すべて再検査の対象**になる。

### 8. 要約は筋に合う方向へ寄る

21で見つけた。OVERVIEW の筋2 は根拠3つのうち**2つが要約の段階で意味を変えていた**。

- 05本文「交差点付近は 0.025〜0.045 の全域で 8/8 と頑健」→ 要約「共存はナイフエッジ」
- 07本文「勝敗がひっくり返るだけで共存自体は保たれる（8/8）」→ 要約「共存がさらに壊れた」

3回とも同じ向き（「共存しない」側）。しかも07は**結論としては当たっていた**ので、
要約を直しても結論が変わらない。**間違った根拠で正しい結論に着いていた**状態は、
自分では気づけない。

対策は1つだけ。**要約を根拠に使う前に、元のレポートの本文を読み直す。**
これは 06 で「知っている現象を当てはめて確認を省いた」のと同じ構造で、
そこでは知識が、ここでは自分が書いた筋が確認を省かせている。

### 9. 周期のある系で相関を読むな

08で「死骸は草食が少ない時期に戻る（相関 -0.74）」と書いたが、
ラグを振ったら**符号が反転し続けた**。両方の系列が同じ周期で振動しているので、
相関は周期を写しているだけだった。どのラグを選ぶかで話が変わる。

### 10. 名前を知っている現象ほど、当てはめる前に測る

06で「豊穣化のパラドックス」を知っていたせいで、それらしいデータを見た時点で
確認を省いて誤報した。07では見立てが3回外れ、09では2回外れている。
**もっともらしい説明を思いつく能力は、当たっているかとは無関係。**

### 11. 実装の都合が生態系の挙動として観察される

03で3回やられた。走査順のバイアスが「視野を持つと群れて非効率になる」に見え、
インデックス構築のタイミングが「視野があると捕食者が不利になる」に見えた。
どれも生態学的にありえそうな話だった。**新しい挙動が出たら、まず実装を疑う。**

## 実験の手順

```bash
node docs/reports/scripts/NN-name.ts   # Node 22+ は .ts を直接実行できる
npx tsc --noEmit                       # @types/node 未導入の TS2591 は既存の警告
```

- 条件は **6000ステップ / 後半3000ステップで統計 / 8シード** に揃える。
  過去の表とそのまま突き合わせられるようにするため。
  ただし**共存や崩壊を判定するときは足りない**（上の7）。30000ステップと相互侵入を足す
- 新しい軸を足したら、**既定値で全6構成が
  [02](docs/reports/02-preset-validation.md) の数字とビット単位で一致する**ことを必ず確認する。
  乱数ストリームを消費すると再現性が壊れるので、追加の乱数は別インスタンスから引く
- レポートには**外した見立ても残す**。当たった結論だけ書くと、
  次に同じ間違いをする

## コードの約束

- `src/core/` は DOM を参照しない。表示側と探索側で同じコードを動かすため
- 乱数は必ず `rng.ts` を通す。`Math.random()` は使わない
- 種を足すのは `presets.ts` だけ。UIのスライダーもグラフも種定義から自動生成される
- エージェントは Structure of Arrays（TypedArray）。オブジェクトの配列にしない
- コメントは「なぜそうしたか」を書く。特に**一度壊れて直した箇所**は経緯を残す

## サブエージェントへの委譲

以下に当てはまる作業は、**都度の確認なしにサブエージェントへ委譲すること**。
これはこのリポジトリにおける恒常的な指示であり、毎回の依頼を必要としない。

理由はコンテキストの再送コスト。会話の全履歴は毎ターン再送されるので、
一度メインの文脈に入れた大きな出力は、以降のやり取りすべてで課金対象になり続ける。
サブエージェントは自分の文脈で作業し、要約だけを返すため、この累積を断ち切れる。

### 委譲するもの

出力が大きく、結論だけあれば足りるもの。このリポジトリでは具体的に以下。

| 作業 | 理由 |
|---|---|
| `npm run sweep` | 36条件×3回×4000ステップで数十秒、条件ぶんの表と CSV が出る |
| `docs/reports/scripts/*.ts` の実行 | レポート用の検証スクリプト。出力が長い |
| 長ステップの生存確認（8000ステップ×複数シード等） | 進捗行と個体数の系列が延々と出る |
| アプリを起動してのスクリーンショット | 画像は1枚1,500〜2,000トークン、しかも一度読むと以降ずっと文脈に残る |
| `npm install` / ビルドの全ログ | 成否と警告だけ分かればいい |
| 広い範囲のコード探索・grep の総なめ | ヒット一覧そのものは不要 |

### 委譲しないもの

- 数ファイルの読み書きで済む小さな変更
- 往復しながら詰める作業（設計の相談、デバッグの試行錯誤）
- 前後の工程と文脈を共有する必要がある作業

サブエージェントは会話履歴を引き継がず、ゼロから文脈を作り直す。
小さい作業では、探索のやり直しのぶんかえって高くつく。

### 使い分け

| エージェント | 用途 |
|---|---|
| `Explore` | 読み取り専用の探索。CLAUDE.md と git status を読まないぶん起動が軽い |
| `Plan` | 実装方針の設計 |
| `general-purpose` | 多段階のタスク、実行を伴う調査 |

単純な集計や検索は `model: haiku` を指定してよい。

### 報告のしかた

サブエージェントの結果をそのまま貼らないこと。生ログ・CSV 全文・進捗行を
メインの文脈に持ち込むと、委譲した意味がなくなる。

返すのは結論だけ。sweep なら「共存した条件と生存率」、検証実行なら
「どのシードで何ステップ目に何が絶滅したか」といった粒度にまとめる。

スクリーンショットは必要な枚数だけ撮る。比較が目的でないなら1枚でよい。
